import React from "react";

function Header  () {
    return (
        <header style={{backgroundColor: "maroon", color: "white", padding: "10px", textAlign: "center"}}>
          flatiron bank
        </header>
    );
}

export default Header;